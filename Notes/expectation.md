expectation = average. you get a function with probability of each outcome happening tie to a score in expectation and it outputs a number, the average
the whole equation in word is the expectation of the proability distribution of the real world data of actually happening from loss?
bias -> underfitting
bias -> cap and training ~ testing
variance -> overfitting
variance is the training noise memorized (like if there are noise in the training then that will translate to testing)
variance -> gap in training > testing
Bias = “I can’t fit the training data.”
Variance = “I fit training too well but don’t generalize.”